import { Box, Container, Typography } from '@mui/material';
import React from 'react';

export function List(): React.ReactElement {
  return (
    <Container>
      <Box>
        <Typography variant="body1">Contents Here</Typography>
      </Box>
    </Container>
  );
}
